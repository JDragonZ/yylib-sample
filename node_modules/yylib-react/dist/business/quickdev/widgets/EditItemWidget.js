'use strict';

var React = require('react');

var _require = require('yylib-ui');

var YYClass = _require.YYClass;

var PropTypes = YYClass.PropTypes;
var field = YYClass.field;

//定义META
var uiMeta = {
    name: 'edititemw',
    fields: [,
    //field参数列表：字段属性名|属性默认值|属性类型|是否必填|属性描述
    field('key', '', PropTypes.string, true, '字段的唯一标识React 需要的 key'), field('dataindex', '', PropTypes.string, true, '列数据在数据项中对应的 key'), field('title', '', PropTypes.string, true, '字段名称'), field('uisubtype', '', PropTypes.string, false, '单元格表单类型'), field('isShow', true, PropTypes.string, false, '单元格是否显示')]
};

var EditItemWidget = YYClass.create({
    uiMeta: uiMeta
});

module.exports = EditItemWidget;