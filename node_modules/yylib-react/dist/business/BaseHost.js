'use strict';

/**
 * 依据环境自判断IP地址
 */
var ADDR;
// 内网环境(生产环境)
if (process.env.YYLIB_ENV == 'prod') {
    //ADDR = "http://10.11.248.46";
    ADDR = "zjy.cscec.com.cn";
}
// 外网环境
else if (process.env.YYLIB_ENV == 'outer') {
        ADDR = "http://60.205.12.0:81";
    }
    // 测试环境
    else if (process.env.YYLIB_ENV == "test") {
            ADDR = "http://60.205.12.0:81";
        }
        // 本地环境
        else {
                // ADDR = "http://127.0.0.1:8080";
                ADDR = "http://60.205.12.0:81";
            };
module.exports = ADDR;