'use strict';

var React = require('react');

var _require = require('yylib-ui');

var YYClass = _require.YYClass;

var PropTypes = YYClass.PropTypes;
var field = YYClass.field;

//定义META
var uiMeta = {
    name: 'searchitem',
    fields: [,
    //field参数列表：字段属性名|属性默认值|属性类型|是否必填|属性描述
    field('uikey', '', PropTypes.string, true, '字段的唯一标识'), field('oper', [], PropTypes.array, true, '字段允许的操作符集合'), field('title', '', PropTypes.string, true, '字段名称'), field('fieldtype', '', PropTypes.string, false, '字段类型'), field('inputtype', '', PropTypes.string, false, '')]
};

var YYSearchItem = YYClass.create({
    uiMeta: uiMeta
});

module.exports = YYSearchItem;